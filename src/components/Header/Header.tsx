export const Header = ({ siteTitle }: { siteTitle: string }) => (
  <header className="bg-white dark:bg-black">
    <button
      className=""
      onClick={() => {
        return
      }}
    >
      다크모드
    </button>
    <button
      className=""
      onClick={() => {
        return
      }}
    >
      노말모드
    </button>
  </header>
)
